extensions = ["sphinx_covsight"]

covsight_plan_name = "citations"
covsight_needs_json = "_spec/needs.json"
covsight_spec_base_url = "https://spec.example/"
covsight_env_policy = {"ip_simulation": {"title": "IP Simulation"}}

exclude_patterns = ["_build"]
